name: Recent Activity

on:
  schedule:
    - cron: "0 * * * *" # Runs every hour
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Update Recent Activity

    steps:
      - uses: Readme-Workflows/recent-activity@v2.6.0
        with:
          GH_USERNAME: S-Sajith
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
